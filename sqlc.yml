version: "2"

sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "database/migrations"

    gen:
      go: 
        package: "storage"
        out: "storage"
        sql_package: "database/sql"
